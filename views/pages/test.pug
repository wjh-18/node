//
   Created by Administrator on 2017/9/2.

extends ../layout

block content
    .container
        .row
            each item in movies
                .col-sm-2
                    a(href=item.poster, title=item/title)
                    .caption
                        h1 #{title}
                        p: